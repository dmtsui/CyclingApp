// re = /^.*\((-?[0-9]+)px, (-?[0-9]+)px, 0\);/;
// pane = $('.leaflet-map-pane');
// trans_coord = pane.attr('style');
// m = trans_coord.match(re);
// 
// 
// [ parseInt( m[1] ), parseInt( m[2] ) ]
